{
  "scripts": {
    "test": "node --max-old-space-size=512 --test --test-concurrency=1 tests/*.spec.js",
    "test:fast": "node --test tests/*.spec.js",
    "benchmark": "node tests/benchmark.js"
  }
}
